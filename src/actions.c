#include "philo.h"

void	*make_actions(void *philo_v)
{
	t_philos	*philo;

	philo = (t_philos *)philo_v;
	while (1)
	{
		// take_forks(philo);
		// make_eat(philo);
		// make_sleep(philo);
		// make_think(philo);
	}
	return (NULL);
}